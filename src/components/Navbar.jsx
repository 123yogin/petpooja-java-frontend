// This component is now replaced by Layout.jsx
// Keeping for backward compatibility but Layout should be used instead
import Layout from "./Layout";

export default function Navbar() {
  return null;
}

